{
  "name": "@pwig/backend",
  "version": "1.0.0",
  "description": "API for Pwig",
  "main": "dist/server.js",
  "scripts": {
    "start": "NODE_ENV=production node dist/server.js",
    "dev": "NODE_ENV=development ts-node-dev --respawn --transpile-only src/server.ts",
    "build": "tsc && cp -r templates dist/"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^4.21.2",
    "puppeteer": "^24.9.0",
    "twig": "^1.17.1"
  },
  "devDependencies": {
    "@types/cors": "^2.8.14",
    "@types/express": "^4.17.23",
    "@types/node": "^22.15.32",
    "@types/twig": "^1.12.16",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.8.3"
  }
}
